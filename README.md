# RC-RL-FilterSimulator
The frequency responses from RC and RL lowpass and highpass filters can be simulated, with respect to generated noise samples. The simulation is interactive and the user can change the R, L, C values when necessary.
